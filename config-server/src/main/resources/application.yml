server:
  port: 8888

spring:
  application:
    name: config-server

  profiles:
    active: native   # de momento usaremos el modo "native" (carpeta local)

  cloud:
    config:
      server:
        native:
          # Aquí buscaremos los .yml de config (en la siguiente parte los llenamos)
          search-locations: classpath:/configs

management:
  endpoints:
    web:
      exposure:
        include: health,info
