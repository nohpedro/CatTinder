# -------------------------
# Identidad del servicio
# -------------------------
spring.application.name=preferences-service

# Puerto propio del microservicio (no 8080 para no chocar con otros)
server.port=8082

# -------------------------
# Swagger / OpenAPI
# -------------------------
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui

# Para que Swagger funcione correctamente detrás del gateway
server.forward-headers-strategy=framework
springdoc.swagger-ui.use-root-path=true

# -------------------------
# Base de datos Postgres
# (usa la MISMA DB cattinder_db que ya creaste)
# -------------------------
spring.datasource.url=jdbc:postgresql://localhost:5432/cattinder_preferences
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# -------------------------
# Eureka Discovery Client
# -------------------------
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.instance.prefer-ip-address=true
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# =========================
# Keycloak / OAuth2 Resource Server
# =========================
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/cattinder

spring.config.import=optional:configserver:http://config-server:8888
